import { CambioImgPipe } from './cambio-img.pipe';

describe('CambioImgPipe', () => {
  it('create an instance', () => {
    const pipe = new CambioImgPipe();
    expect(pipe).toBeTruthy();
  });
});
